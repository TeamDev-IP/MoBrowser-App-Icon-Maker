# MoBrowser-App-Icon-Maker

Before you launch the app, run this command to install the dependencies:

```bash
cd /Users/vladimir/Projects/IconMaker
python3 -m venv .venv
./.venv/bin/pip install -r assets/resources/python/requirements.txt
```